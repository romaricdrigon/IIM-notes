<!doctype html>
<html>
    <head></head>
    <body>
        <h1>Add a student</h1>

        {{ form_start(form) }}
            {{ form_rest(form) }}

            <input type="submit" value="Save" />
        {{ form_end(form) }}
    </body>
</html>